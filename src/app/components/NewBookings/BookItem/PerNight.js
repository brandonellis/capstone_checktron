import React, {Component} from 'react'

export default class PerNight extends Component{
  render(){
    return(
      <div>
        PerNight
        {
          //Check-in
          // if len is 0 Check-out
          //param, hide qty if stock is 1
        }
      </div>
    )
  }
}
