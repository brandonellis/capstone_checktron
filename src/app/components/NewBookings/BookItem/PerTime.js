import React, {Component} from 'react'

export default class PerTime extends Component{
  render(){
    return(
      <div>
        PerTime
        {
          // Date
          // Time (start)
          // if len is 0 Time (end) :: calculate time with len start and unit
          // param, hide qty if stock is 1
        }
      </div>
    )
  }
}
